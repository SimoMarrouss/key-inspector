<!-- Plugin Configuration File: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN, cannot be changed between the plugin versions. -->
    <id>com.usehashmap.key-inspector</id>

    <!-- Public plugin name should be written in Title Case. -->
    <name>Key Inspector</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://github.com/usehashmap">usehashmap</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager. -->
    <description><![CDATA[
        <b>Key Inspector</b> — View keystore and certificate details directly inside your IDE.<br><br>
        Inspired by <em>Keystore Explorer</em>, this plugin lets you open and inspect keystores and
        certificate files without leaving your development environment.<br><br>
        <b>Supported keystore formats:</b> JKS, JCEKS, BKS, PKCS#12 (p12/pfx), UBER, BCFKS<br>
        <b>Supported certificate / key formats:</b>
        PEM, DER (cer, crt), PKCS#7 (p7/p7b), PKCS#8, PKCS#10 (p10),
        PVK, PubKey (pub), PrivKey (key), pkipath, SPC, SPKAC, CRL
    ]]></description>

    <!-- Product and plugin compatibility requirements. -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.compose</depends>

    <resource-bundle>messages.KeyInspectorBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Tool window shown in the side bar -->
        <toolWindow id="Key Inspector"
                    factoryClass="com.usehashmap.keyinspector.KeyInspectorToolWindowFactory"
                    anchor="right"
                    icon="AllIcons.Toolwindows.ToolWindowPalette"/>

        <!--
            File editor provider: opening a certificate / keystore file from the
            project tree (or via Navigate > File) shows the Key Inspector UI
            instead of the raw text/binary editor.
        -->
        <fileEditorProvider
                implementation="com.usehashmap.keyinspector.filetype.KeyInspectorFileEditorProvider"/>

        <!-- Register BouncyCastle security providers at startup -->
        <postStartupActivity
                implementation="com.usehashmap.keyinspector.BouncyCastleStartupActivity"/>

        <!-- Notification group for import success balloons -->
        <notificationGroup id="Key Inspector"
                           displayType="BALLOON"
                           isLogByDefault="true"/>
    </extensions>

    <actions>
        <!-- ── Tools menu entry ──────────────────────────────────────────── -->
        <group id="KeyInspector.ToolsMenu"
               text="Key Inspector"
               description="Key Inspector actions"
               popup="true">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
            <action id="KeyInspector.ImportCert"
                    class="com.usehashmap.keyinspector.actions.ImportCertAction"
                    text="Import Certificate / Key Pair…"
                    description="Import a certificate or key pair into a keystore"/>
        </group>

        <!-- ── Project-view context menu (right-click on a keystore file) ── -->
        <action id="KeyInspector.ImportCert.ProjectView"
                class="com.usehashmap.keyinspector.actions.ImportCertAction"
                text="Import Certificate / Key Pair…"
                description="Import a certificate or key pair into this keystore">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
